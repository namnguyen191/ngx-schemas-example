# A simple library with support for Angular schematics

## To try it out

1. Install and run [Verdaccio](https://verdaccio.org/)
2. Run `npm run build-and-publish`
3. Create another Angular porject with an `.npmrc` that point to Verdaccio local server
4. Try `ng add my-ui` in that prjects!
